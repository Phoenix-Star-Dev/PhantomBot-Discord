module.exports = {
  async execute(interaction) {
    await interaction.reply("📦 Bundling your transactions... (dummy for now)");
    // Later: accept transactions, send to solana/bundler.js
  },
};
